name: 'Release Drafter'

on:
  push:
    branches:
      - master

jobs:
  update_release_draft:
    name: Release
    uses: chukysoria/workflows/.github/workflows/new-release.yml@v1.0.18
    with:
      tag-external-version: true
