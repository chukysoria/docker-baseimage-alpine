name: 'Release Drafter'

on:
  push:
    branches:
      - master
      - edge

jobs:
  update_release_draft:
    name: Release
    uses: chukysoria/workflows/.github/workflows/new-release.yml@v1.0.26
    with:
      tag-external-version: true
