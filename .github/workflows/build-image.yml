name: Build Image

on:
  release:
    types: [published]
  push:
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build and deploy
    uses: chukysoria/workflows/.github/workflows/build-image.yml@cd870671c263b80aec39e2332cbbc02cef9287ad # v1.1.19
    with:
      tag-external-version: true
