name: Build Image

on:
  release:
    types: [published]
  push:

env:
  VERSION: "3.18"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set current date as env variable
        run: echo "NOW=$(date +'%Y-%m-%d')" >> $GITHUB_ENV

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
        with:
          platforms: arm

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
        with:
          platforms: linux/arm/v7

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          # list of Docker images to use as base name for tags
          images: |
             ghcr.io/chukysoria/alpine            
          # generate Docker tags based on the following events/attributes
          tags: |
            type=sha,format=long
            type=raw,value=${{ github.ref_name }}
            type=ref,event=branch,prefix=${{ env.VERSION }}-
            type=ref,event=tag,prefix=${{ env.VERSION }}-
          labels: |
            org.opencontainers.image.created=${{ env.NOW }}
            org.opencontainers.image.authors=Carlos Sanchez
            org.opencontainers.image.url=https://github.com/chukysoria/docker-baseimage-alpine/packages
            org.opencontainers.image.documentation=https://docs.linuxserver.io/images/docker-baseimage-alpine
            org.opencontainers.image.source=https://github.com/chukysoria/docker-baseimage-alpine
            org.opencontainers.image.version=${{ github.ref_name }}-ls
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.vendor=Carlos Sanchez
            org.opencontainers.image.licenses=GPL-3.0-only
            org.opencontainers.image.ref.name=${{ github.sha }}
            org.opencontainers.image.title=Baseimage-alpine
            org.opencontainers.image.description=baseimage-alpine image by Carlos Sanchez        

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          push: ${{ github.event_name == 'release' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            OS=armhf
            VERSION=${{ github.ref_name }}
            BUILD_DATE=${{ env.NOW }}
